name: SauceDemo Regression Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Code auschecken
      uses: actions/checkout@v4

    - name: Java 21 einrichten
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'maven'

    - name: Tests im Headless-Modus ausführen
      # Wir übergeben zusätzliche Parameter, um Chrome stabil zu halten
      run: mvn test -Dbrowser=chrome-headless -DargLine="-Dchrome.options='--headless --no-sandbox --disable-dev-shm-usage'"

    - name: Test-Report hochladen
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: cucumber-report
        path: target/cucumber-reports/